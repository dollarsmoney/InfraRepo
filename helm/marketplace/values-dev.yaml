environment: dev
backend:
  image:
    tag: sha-19c2158
  replicaCount: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 300m
      memory: 384Mi
frontend:
  image:
    tag: sha-19c2158
  replicaCount: 1
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 300m
      memory: 384Mi
config:
  NODE_ENV: production
  SUPABASE_URL: https://gwwasgxuhoeowhswqooj.supabase.co
ingress:
  # Browsers block fetch() to a host with an untrusted cert, so dev needs a
  # real certificate for the frontend to reach api.dev.*.
  clusterIssuer: letsencrypt-production
  hosts:
    app: dev.kmdndd.name.ng
    api: api.dev.kmdndd.name.ng
